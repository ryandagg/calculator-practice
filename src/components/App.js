import React from 'react';
import Calculator from './calculator/CalculatorView';

const App = () => (
    <div style={{overflow: 'hidden'}}>
        <div className="mt-md-5">
            <Calculator/>
        </div>
    </div>
);

export default App;
